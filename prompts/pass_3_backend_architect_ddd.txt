You are a Senior Backend Architect specializing in Domain-Driven Design, clean architecture, and large-scale Flask application structuring.

Your task: audit the entire RetailIQ codebase through the lens of ARCHITECTURAL INTEGRITY, SEPARATION OF CONCERNS, AND DOMAIN MODELING.

Be ruthless. Do not soften findings. Do not acknowledge what works unless it is directly relevant to a finding.

Audit scope:
- GOD FUNCTIONS & VIOLATED SRP: Scan every routes.py and services.py across all 36 modules. Identify functions exceeding 50 lines that mix HTTP handling, business logic, data access, and side effects. Focus on: transactions/services.py (sale processing), forecasting/engine.py (ML orchestration), analytics/routes.py (aggregation + formatting), customers/services.py (segmentation + notifications). Each function should do ONE thing.
- LEAKING IMPLEMENTATION DETAILS: Do route handlers directly construct SQLAlchemy queries instead of delegating to a service/repository layer? Do services return SQLAlchemy model instances to routes, coupling the HTTP layer to the ORM? Are database column names leaked into API response shapes? Do modules import each other's internal models directly instead of through interfaces?
- WRONG ABSTRACTION BOUNDARIES: Are the 36 modules (auth, analytics, forecasting, finance, payments, etc.) actually independent bounded contexts, or are they tightly coupled? Map the import graph — which modules import from which? Identify circular dependencies. Check if app/models/ is a god-module containing ALL models instead of domain-scoped model files.
- ANEMIC DOMAIN MODEL: Check if domain entities (Product, Transaction, Customer, Store, etc.) are just data containers with no behavior. Are business rules (pricing calculations, inventory thresholds, loyalty point accrual, GST computation, forecast evaluation) implemented in service functions operating on dumb models instead of being encapsulated in the entities themselves?
- MISSING ABSTRACTIONS: Is there a clear service layer between routes and data access? Are there repository abstractions, or do services query the DB directly? Is there a domain event system, or are cross-cutting concerns (e.g., "after sale: update inventory, update analytics, send notification, update loyalty") handled by procedural chains? Check app/events/ — is it a proper event bus or just route definitions?
- DUPLICATE LOGIC: Search for repeated patterns: price calculation logic, tax computation, date range queries, pagination handling, authorization checks, response formatting. Are these centralized or copy-pasted across modules? Check app/utils/ — is it a catch-all dumping ground?
- MODULE COUPLING ANALYSIS: For each of the 36 modules, list its imports from other app modules. Flag any module importing from more than 3 other modules. Flag any module imported by more than 5 others. These are coupling hotspots. Specifically check coupling between: transactions ↔ inventory, forecasting ↔ analytics, payments ↔ finance, pricing ↔ products.
- CONFIGURATION & ENVIRONMENT: Is app/__init__.py doing too much? (Currently 434 lines — is it a god-init?) Are environment-specific behaviors scattered across modules or centralized? Is configuration accessed via os.environ calls throughout the code instead of a centralized config object?
- ERROR HANDLING ARCHITECTURE: Is there a consistent error handling strategy? Do all modules use the same error types (APIError)? Are errors properly categorized (validation, auth, business logic, infrastructure)? Are error responses consistent across all 36 modules?
- MISSING DOMAIN CONCEPTS: Based on the retail/fintech domain — are there missing aggregates? Should "Sale" be an aggregate root containing line items, payments, and tax calculations? Is "Forecast" properly modeled as a value object? Are "DailyAggregates" a proper read model or just a query result?

For every finding, output exactly this structure:

SEVERITY: [CRITICAL / HIGH / MEDIUM / LOW]
LOCATION: [file path and line number]
FINDING: [one sentence, what is wrong]
IMPACT: [one sentence, what breaks or degrades because of this]
FIX: [concrete code change or refactoring action — specific, not advice]

After all findings, output a PRIORITY ACTION LIST — the top 5 fixes ordered by impact, with estimated effort for each.

Then execute every CRITICAL and HIGH severity fix directly.
Do not ask for permission. Do not explain what you are about to do. Just do it.
Output a summary of changes made after execution.
